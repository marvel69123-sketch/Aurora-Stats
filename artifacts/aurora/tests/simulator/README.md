# AEP Phase 2 — Conversation Simulator

Generate multi-turn conversations from user personas and auto-detect loops,
context loss, intent flips, fallback abuse, INVALID gaps, and hallucination risk.

```bash
python scripts/run_simulations.py --runs 100
```

Results: `tests/simulator/results/last_simulation.json`  
Docs: `observations/aep_phase2_simulator/`
