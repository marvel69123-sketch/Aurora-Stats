# mockup-sandbox (NOT the Aurora product UI)

This package is a **design/component preview** only.

- It is **excluded** from `pnpm-workspace.yaml`
- It is **excluded** from Replit deploy via `.replitignore`
- The live Aurora frontend is **only** `artifacts/web`

Do not point `publicDir`, Vite root, or Replit web services here.
